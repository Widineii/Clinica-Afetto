# Copie para local-mail.env (nao vai pro Git):
#   copy scripts\local-mail.env.example scripts\local-mail.env
#
# Gmail widineimartins4@gmail.com — crie senha de app em:
# https://myaccount.google.com/apppasswords
# (Conta Google > Seguranca > Verificacao em duas etapas > Senhas de app)
# Depois: trocar para afettopsicologiaesaude@gmail.com quando a clinica estiver pronta

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=widineimartins4@gmail.com
MAIL_PASSWORD=COLE_A_SENHA_DE_APP_AQUI

RECUPERACAO_SENHA_MODO_CONSOLA=false
AVISO_PAGAMENTO_EMAIL_MODO_CONSOLA=false

# PowerShell antes de subir o servidor:
#   Get-Content scripts\local-mail.env | ForEach-Object {
#     if ($_ -match '^\s*([^#][^=]+)=(.*)$') { Set-Item -Path "env:$($matches[1].Trim())" -Value $matches[2].Trim() }
#   }
#   .\mvnw.cmd spring-boot:run "-Dspring-boot.run.profiles=local"
